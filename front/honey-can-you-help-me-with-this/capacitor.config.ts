import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.org.casualengineer',
  appName: 'honey-can-you-help-me-with-this',
  webDir: 'dist'
};

export default config;
